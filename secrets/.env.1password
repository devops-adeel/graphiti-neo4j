# 1Password Secret Template for Graphiti-Neo4j
# This file contains references to secrets stored in 1Password
# Vault: HomeLab (personal infrastructure vault)
#
# Usage:
#   op inject -i secrets/.env.1password -o /tmp/.env-$$
#
# Prerequisites:
# 1. Install 1Password CLI: brew install --cask 1password-cli
# 2. Sign in: op signin
# 3. Setup vault: make setup-vault

# =============================================================================
# Neo4j Core Authentication
# =============================================================================

# Neo4j Credentials (auto-generated on setup)
NEO4J_USER=neo4j
NEO4J_PASSWORD=op://HomeLab/Graphiti-Neo4j/password

# =============================================================================
# Integration APIs
# =============================================================================

# OpenAI API Key (existing item in your vault)
# Using vault UUID and item UUID for stability
OPENAI_API_KEY=op://ywvjwtjo75i2xxb5pol2lzmkmy/toyqxan47kodn5lwhp7zphsdka/credential

# =============================================================================
# Connection URIs - OrbStack Only
# =============================================================================

# Primary connection URI for Neo4j Bolt protocol
NEO4J_URI=bolt://neo4j.graphiti.local:7687

# OrbStack-specific URIs
NEO4J_URI_ORBSTACK=bolt://neo4j.graphiti.local:7687
NEO4J_URI_DOCKER=bolt://neo4j-graphiti:7687

# Browser Access - OrbStack domains only (no localhost)
NEO4J_BROWSER=http://neo4j.graphiti.local:7474
NEO4J_BROWSER_ORBSTACK=http://neo4j.graphiti.local:7474

# =============================================================================
# Notes
# =============================================================================
# - This file is safe to commit (contains only references)
# - Actual secrets are stored securely in 1Password
# - No localhost references - pure OrbStack networking
# - Ports in URLs are for OrbStack routing to container ports